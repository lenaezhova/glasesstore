import {theme} from 'antd';

export const lightTheme = {
  algorithm: theme.defaultAlgorithm,
  token: {
    colorPrimary: '#7a6cfd',
    wireframe: true
  }
};
export const darkTheme = {
  algorithm: theme.darkAlgorithm,
  token: {
    colorBgBase: '#00132C',
    // colorBgContainer:'#00132C',
    colorPrimary: 'rgba(135, 216, 200, 0.7)',
    wireframe: true
  }
};
